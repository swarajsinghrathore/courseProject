export class Ingredient {
    // public name: string;
    // public amount: number;
    //Another way...in this method we don't need to use this.name=name it will automatically did
    constructor(public name:string, public amount:number){}
}